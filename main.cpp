//main function
#include <iostream>
#include <map>
#include <vector>

#include <code-notes.h>

int main() {
  std::string stringValue = "this is a string";
  std::double input;

  std::cout << std::ws << stringvalue;         // std::ws represents "whits space" (ex: " ")
                                               // std::cout represents an output to terminal
  std::cin >> input;                           // std::cin represtns and input from terminal -> sets it to variable presented

  if (input == stringValue)                    // boolean check in "if" statement
  {
    cout << "this is the same";
  }

  myClass person{};                            // calling a class called "person"
  
}
